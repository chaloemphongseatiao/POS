# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

ผู้ใช้หลักคือเจ้าของร้านและผู้ดูแล (role `ADMIN`) ซึ่งต้องควบคุมการขาย สินค้า stock รายการขาย และภาพรวมของร้านจากระบบเดียว ผู้ใช้รองคือแคชเชียร์ (role `CASHIER`) ที่ต้องทำรายการขายหน้าร้านให้จบเร็วและถูกต้อง ระบบต้องใช้งานได้ทั้งจอ desktop และ responsive mobile web

## Product Purpose

POS นี้เป็น internal tool ที่สร้างให้ตรงกับขั้นตอนทำงานของร้าน เป้าหมายหลักคือขายสินค้าได้เร็วขึ้นและคำนวณยอดเงินได้ถูกต้อง ตั้งแต่เลือกสินค้า รับชำระเงิน ออกใบเสร็จ ไปจนถึงเก็บข้อมูลรายการขายและ stock

## Positioning

Internal POS ภาษาไทยที่ออกแบบตามขั้นตอนของร้านเอง เพื่อให้เจ้าของร้านควบคุมงานได้ชัดและให้แคชเชียร์ขายได้เร็วโดยลดความผิดพลาดด้านยอดเงิน

## Operating Context

- ขอบเขตปัจจุบัน: ร้านเดียว หลายเครื่อง แคชเชียร์หลายจุดใช้ข้อมูลชุดเดียวกัน ยังไม่รองรับหลายสาขา
- อุปกรณ์หน้าร้าน: PC/Notebook พร้อมเมาส์คีย์บอร์ด, tablet จอสัมผัส และเครื่องสแกนบาร์โค้ด ทั้งสองรูปแบบต้องขายได้เต็มงาน
- ช่องทางชำระเงิน: เงินสด (ต้องแสดงเงินรับ/เงินทอน) และ QR PromptPay หรือโอน ยังไม่มีบัตรเครดิต/เดบิต
- ยังไม่มีเครื่องพิมพ์ใบเสร็จในระบบ ใบเสร็จอยู่บนหน้าจอ
- แจ้งเตือนคำสั่งซื้อผ่าน LINE Messaging API เป็น side effect หลังบันทึกออร์เดอร์ ไม่บล็อกการขาย
- หน้างานหลัก: `pos`, `products`, `orders`, `dashboard`, `settings` และ login

## Capabilities and Constraints

- โดเมนที่มีจริงใน API: `auth`, `users`, `categories`, `products`, `stock`, `orders`, `reports`, `settings`, `line`
- สิทธิ์มี 2 ระดับเท่านั้น: `ADMIN` และ `CASHIER` (JWT bearer token)
- ออร์เดอร์บันทึกใน transaction เดียว และ snapshot `unitPrice` / `costPrice` ลง `OrderItem` เพื่อไม่ให้ราคาย้อนหลังเปลี่ยน
- การขาย **ไม่** ตัด stock อัตโนมัติ เป็นการตัดสินใจโดยตั้งใจ อย่าแก้ให้ "ถูกต้อง" เอง
- ค่าเงินเป็น Prisma `Decimal` และส่งผ่าน API เป็น string ตลอด — การจัดรูปแบบตัวเลขเป็นงานของ frontend
- ชื่อร้านเก็บเป็น setting `store_name` (key-value ใน `Setting`) ไม่ใช่ค่าคงที่ใน code
- ข้อความ error ที่ผู้ใช้เห็นเป็นภาษาไทย
- Open decision: หลายสาขา, เครื่องพิมพ์ใบเสร็จ, บัตรเครดิต/เดบิต ยังไม่ตัดสินใจ

## Brand Commitments

- มีชื่อร้านจริงแล้ว แต่ตั้งค่าที่ runtime ผ่าน `store_name` ไม่ใช่ hardcode
- ยังไม่มีไฟล์โลโก้หรือ asset แบรนด์ในโปรเจกต์ อย่าสร้างโลโก้สมมติ
- น้ำเสียง: เร็ว · ชัด · ไว้ใจได้ ตรง เข้าใจง่าย เน้นข้อมูลที่ใช้ตัดสินใจหรือทำงานต่อได้ทันที ทุกหน้าให้ความรู้สึกมั่นคงและคาดเดาได้ โดยเฉพาะขั้นตอนขาย ชำระเงิน และแก้ stock

## Evidence on Hand

- โค้ดจริงทั้ง `WebAPI/` และ `WebApp/` พร้อม seed data ใน `WebAPI/prisma/seed*.ts`
- `WebApp/NewDesing/` เป็น static HTML mockup ใช้อ้างอิงได้ แต่ไม่ใช่ส่วนของแอปที่ build
- ยังไม่มียอดขายจริง ตัวเลขผลลัพธ์ รีวิว หรือ case study — อย่ากุตัวเลขหรือคำรับรองขึ้นมา

## Product Principles

1. **งานขายต้องเร็ว:** ลดจำนวนขั้น กดง่าย สแกนง่าย และให้ primary action ชัดในจังหวะที่ต้องใช้
2. **ยอดเงินต้องไว้ใจได้:** แสดงราคา จำนวน ส่วนลด ยอดรวม เงินรับ และเงินทอนอย่างชัดเจน พร้อม feedback หลังทำรายการ
3. **ข้อมูลต้องอ่านทันที:** ใช้ภาษาไทยสั้นตรง ลำดับชั้นชัด และจัดตัวเลขให้เทียบกันง่าย
4. **รูปแบบต้องสม่ำเสมอ:** action เดียวกันใช้ component, label และ state แบบเดียวกันทุกหน้า
5. **Responsive ตามงาน:** รักษางานหลักให้ครบทั้ง desktop, tablet จอสัมผัส และ mobile โดยปรับโครงสร้างตามพื้นที่ ไม่ลดเพียงขนาดตัวอักษร

## Accessibility & Inclusion

เป้าหมายคือ WCAG 2.2 AA รองรับ keyboard navigation (แคชเชียร์บางเครื่องใช้คีย์บอร์ดกับเครื่องสแกนบาร์โค้ดเป็นหลัก) และมี focus indicator ที่ชัด รองรับ reduced motion ใช้ touch target อย่างน้อย 44 × 44 px สำหรับ action หลักบนจอสัมผัส และไม่ใช้สีอย่างเดียวเพื่อสื่อสถานะหรือความผิดพลาด ข้อความและตัวเลขสำคัญต้องมี contrast เพียงพอและอ่านได้ในสภาพแสงของหน้าร้าน
